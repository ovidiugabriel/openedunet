The _Config_ class is a *Key-Value-Type store* that means it is a key-value store but each value caries its own associated type.

Without this feature each time you request a field value from database it is a string. To get a correct assumed variable type, you have to convert it each time to the assumed type.

 to avoid assuming the correct type that you have to conve

 automatically get structures that are serialized in a specific form in the database

{{{
public mixed Config::get(string key) [static] 
}}}