A client library (ECMA Script) for the BareboneMVC framework.

== `val()` ==

{{{
variant val(string selector, variant value) // Setter
variant val(string selector) // Getter
}}}

|| *`id`* || ... ||
|| *`value`* || ... ||
|| *returns* || ... ||