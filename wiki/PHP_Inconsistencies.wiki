=== `var_dump()` ===

`var_dump()` outputs sometimes `string '' (length=0)` and sometimes `string(0) ''` for an empty string.

== Case sensitivity ==

  # variable names and constants names (including defines) are *case sensitive*
  # function names are class names are *not* case sensitive